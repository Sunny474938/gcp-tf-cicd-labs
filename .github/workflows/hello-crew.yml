name: Hello Crew

on:
  push:
    branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the blueprint
        uses: actions/checkout@v4

      - name: Report for duty
        run: echo "The crew has arrived on site."

      - name: Show today's date
        run: date